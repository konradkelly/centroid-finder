/*
The findConnectedGroups method finds groups of 1s in a 2D integer array. It returns key
information about those groups: the size() of the blob and its centroid value, 
represented by (x, y), where x is the sum of x coordinates together then divided by the group size
and y is the sum of y coordinates divided by the group size. Make sure to
dive them with INTEGER DIVISION.

Blobs are connected to each other horizontally and vertically. Not diagonally.
*/